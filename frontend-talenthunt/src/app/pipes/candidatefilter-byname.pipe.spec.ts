import { CandidatefilterBynamePipe } from './candidatefilter-byname.pipe';

describe('CandidatefilterBynamePipe', () => {
  it('create an instance', () => {
    const pipe = new CandidatefilterBynamePipe();
    expect(pipe).toBeTruthy();
  });
});
